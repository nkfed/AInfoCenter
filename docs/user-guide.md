# Посібник користувача Clawd Bot

> **Аудиторія:** Бізнес-аналітики, DevOps інженери  
> **Складність:** Середній рівень  
> **Версія:** 1.0 (2026-01-30)

---

## 📖 Зміст

1. [Швидкий старт](#швидкий-старт)
2. [Запуск та оновлення](#запуск-та-оновлення)
3. [CLI команди](#cli-команди)
4. [Dashboard та моніторинг](#dashboard-та-моніторинг)
5. [Робота з агентом](#робота-з-агентом)
6. [Git Workflow](#git-workflow)
7. [Troubleshooting](#troubleshooting)

---

## 🚀 Швидкий старт

### Перша конфігурація

```bash
# 1. Перевірка статусу системи
clawdbot doctor

# 2. Застосування автовиправлень
clawdbot doctor --fix

# 3. Запуск gateway
systemctl --user start clawdbot-gateway.service

# 4. Перевірка статусу
systemctl --user status clawdbot-gateway.service
```

### Доступ з Windows (через SSH Port Forwarding)

⚠️ **Важливо:** WSL IP-адреса змінюється після кожного перезавантаження Windows!

**Крок 1: Дізнатися актуальний WSL IP**

```bash
# У WSL терміналі
ip addr show eth0 | grep "inet " | awk '{print $2}' | cut -d/ -f1

# Або з Windows PowerShell
wsl hostname -I
```

**Крок 2: Запустити SSH Port Forwarding**

```bash
# У Windows PowerShell (замінити <WSL_IP> на актуальний)
ssh -N -L 18789:127.0.0.1:18789 root@<WSL_IP>

# Приклад:
ssh -N -L 18789:127.0.0.1:18789 root@172.23.77.240
```

**Пояснення:**
- `-N` — не виконувати команди, тільки форвардинг
- `-L` — локальний порт 18789 → WSL порт 18789
- IP змінюється після кожного reboot

**Результат:** Gateway доступний на `http://localhost:18789`

---

## 🔄 Запуск та оновлення

### Управління сервісом

| Команда | Опис |
|---------|------|
| `systemctl --user start clawdbot-gateway.service` | Запустити gateway |
| `systemctl --user stop clawdbot-gateway.service` | Зупинити gateway |
| `systemctl --user restart clawdbot-gateway.service` | Перезапустити gateway |
| `systemctl --user status clawdbot-gateway.service` | Статус сервісу |
| `systemctl --user enable clawdbot-gateway.service` | Автозапуск при старті |
| `systemctl --user disable clawdbot-gateway.service` | Вимкнути автозапуск |

### Скрипт оновлення

```bash
cd /root/clawd
./update_bot.sh
```

**Що робить скрипт:**
1. Виконує `git pull` для синхронізації з GitHub
2. Перезапускає `clawdbot-gateway.service`
3. Виводить підтвердження завершення

**Альтернативний спосіб (ручний):**
```bash
cd /root/clawd
git pull origin main
systemctl --user restart clawdbot-gateway.service
```

### Ручний запуск (foreground)

**Основний варіант:**
```bash
cd /root/clawd
./run.sh
```

**Debug режим:**
```bash
cd /root/clawd
./start_bot.sh
```

**Переваги foreground запуску:**
- Бачите логи в реальному часі
- Ctrl+C для швидкої зупинки
- Корисно для налагодження

**Недоліки:**
- Зупиняється при закритті терміналу
- Потребує відкритої SSH сесії

---

## 💻 CLI команди

### Основні операції

#### Статус системи

```bash
# Перевірка конфігурації та health checks
clawdbot doctor

# Детальна інформація
clawdbot doctor --verbose
```

**Приклад виводу:**
```
◇  Doctor changes ───────────────────────────╮
│                                            │
│  Telegram configured, not enabled yet.     │
│  Google Chat configured, not enabled yet.  │
│                                            │
├────────────────────────────────────────────╯
```

#### Перегляд логів

```bash
# Останні 200 рядків
clawdbot logs

# Режим реального часу (tail -f)
clawdbot logs --follow

# Збільшити ліміт рядків
clawdbot logs --limit 500

# JSON формат
clawdbot logs --json

# Plain text (без кольорів)
clawdbot logs --plain
```

**Опції:**
| Параметр | Опис | Значення за замовчуванням |
|----------|------|---------------------------|
| `--limit` | Максимум рядків | 200 |
| `--max-bytes` | Максимум байтів | 250000 |
| `--follow` | Слідкувати за оновленнями | false |
| `--interval` | Інтервал polling (мс) | 1000 |
| `--json` | JSON вивід | false |
| `--plain` | Без ANSI стилізації | false |

#### Версія та інформація

```bash
# Версія Clawdbot
clawdbot --version
# Вивід: 2026.1.24-3

# Довідка
clawdbot --help

# Довідка по конкретній команді
clawdbot agent --help
```

---

## 🤖 Робота з агентом

### Постановка завдань

#### Базовий синтаксис

```bash
clawdbot agent --message "Ваше повідомлення"
```

#### Приклади використання

**1. Проста задача:**
```bash
clawdbot agent --message "Яка погода сьогодні?"
```

**2. З вказівкою каналу:**
```bash
clawdbot agent --channel telegram --message "Статус оновлення"
```

**3. З вказівкою thinking level:**
```bash
clawdbot agent --message "Проаналізуй логи за останній тиждень" --thinking high
```

**4. З вказівкою конкретного агента:**
```bash
clawdbot agent --agent ops --message "Summarize system logs"
```

**5. З доставкою у канал:**
```bash
clawdbot agent \
  --to +380123456789 \
  --channel telegram \
  --message "Нагадування: зустріч о 15:00" \
  --deliver
```

**6. JSON вивід:**
```bash
clawdbot agent --message "Системний звіт" --json
```

### Thinking Levels

| Рівень | Опис | Використання |
|--------|------|--------------|
| `off` | Без аналізу | Прості запити |
| `minimal` | Мінімальний аналіз | Факти, короткі відповіді |
| `low` | Базовий аналіз | Стандартні задачі |
| `medium` | Поглиблений аналіз | Складні питання |
| `high` | Максимальний аналіз | Стратегічне планування |

### Канали доставки

Доступні канали:
- `telegram` — Telegram Bot ✅ (основний)
- `whatsapp` — WhatsApp (за замовчуванням)
- `discord` — Discord
- `googlechat` — Google Chat ⚠️ (відключений)
- `slack` — Slack
- `signal` — Signal
- `imessage` — iMessage
- `matrix` — Matrix
- інші...

### Локальний режим

```bash
# Запуск агента локально (без Gateway)
clawdbot agent --local --message "Тестове повідомлення"
```

**Примітка:** Потребує API ключів у змінних оточення (не використовує Gateway credentials).

---

## 📊 Dashboard та моніторинг

### Генерація авторизованого посилання

```bash
clawdbot dashboard
```

**Що відбувається:**
1. Генерується одноразове посилання з токеном авторизації
2. Автоматично відкривається браузер
3. Доступ до Control UI на `https://dashboard.clawd.bot`

**Відключити автовідкриття:**
```bash
clawdbot dashboard --no-open
```

**Приклад виводу:**
```
🦞 Dashboard URL (valid for 1 hour):
https://dashboard.clawd.bot/auth?token=[JWT_TOKEN_HIDDEN]
```

### Використання порту 18789

**Локальний доступ:**
```bash
curl http://localhost:18789/health
```

**Через браузер (після SSH forwarding):**
```
http://localhost:18789/canvas/
```

### Canvas UI — Інтерактивні віджети

**Доступ:** `http://localhost:18789/canvas/` (після SSH forwarding)

**Призначення:**
- Розробка інтерактивних UI-віджетів для візуалізації даних
- Створення кастомних дашбордів прямо в чаті бота
- Тестування user actions перед інтеграцією
- Відображення аналітичних звітів у графічному вигляді

**Як використовувати:**
1. Відкрити `canvas/index.html` у VS Code
2. Створити HTML віджет з потрібною візуалізацією
3. Бот може відправляти посилання на Canvas в Telegram
4. Користувач бачить інтерактивний інтерфейс замість тексту

### Можливості Dashboard

#### 1. **Direct Chat**
- Інтерактивна розмова з агентом
- Реал-тайм відповіді
- Історія сесії

#### 2. **Thinking Monitor**
- Перегляд "думок" агента
- Процес прийняття рішень
- Використані інструменти

#### 3. **Interventions (втручання)**
- Зупинка поточного завдання
- Коригування напрямку
- Примусове завершення

#### 4. **Sessions**
- Історія всіх сесій
- Пошук по датах
- Експорт логів

#### 5. **System Metrics**
- CPU/Memory usage
- Кількість активних агентів
- Статус каналів

---

## 🔧 Git Workflow

### Синхронізація між Windows та Linux

#### Стандартний workflow

**На Windows (VS Code):**
```bash
# 1. Внести зміни у файли
# 2. Додати до staging
git add .

# 3. Створити commit
git commit -m "Опис змін"

# 4. Відправити на GitHub
git push origin main
```

**На Linux (WSL):**
```bash
# Отримати останні зміни
cd /root/clawd
git pull origin main

# Або через скрипт
./update_bot.sh
```

#### Перевірка статусу

```bash
# Поточний стан репозиторію
git status

# Історія комітів
git log --oneline -10

# Різниця з віддаленим репозиторієм
git fetch
git diff origin/main
```

#### Вирішення конфліктів

```bash
# При конфлікті під час pull
git status                  # Побачити конфліктні файли
nano <conflicted_file>      # Відредагувати вручну
git add <conflicted_file>   # Позначити як вирішений
git commit -m "Merge conflict resolved"
git push
```

#### Відкат змін

```bash
# Відкинути локальні зміни (unstaged)
git checkout -- <file>

# Відкатити останній commit (локально)
git reset --soft HEAD~1

# Жорсткий відкат до конкретного commit
git reset --hard <commit_hash>
```

### Регламент синхронізації

**Щоденний workflow:**

1. **Ранок (початок роботи):**
   ```bash
   cd /root/clawd
   ./update_bot.sh
   ```

2. **Протягом дня (після змін):**
   ```bash
   git add -A
   git commit -m "feat: опис нової функції"
   git push
   ```

3. **Вечір (завершення роботи):**
   ```bash
   git status
   git push  # якщо є незакомічені зміни
   ```

**Автоматизація (опціонально):**
```bash
# Додати до crontab для автоматичного pull кожні 30 хв
*/30 * * * * cd /root/clawd && git pull --rebase origin main
```

---

## 🛠️ Troubleshooting

### Проблема 1: Gateway не запускається

**Симптоми:**
```
Gateway start blocked: set gateway.mode=local (current: unset)
```

**Рішення:**
```bash
clawdbot doctor --fix
systemctl --user restart clawdbot-gateway.service
```

---

### Проблема 2: Telegram не відповідає

**Симптоми:** Бот не реагує на повідомлення у Telegram.

**Діагностика:**
```bash
# 1. Перевірити статус сервісу
systemctl --user status clawdbot-gateway.service

# 2. Переглянути логи
clawdbot logs --follow

# 3. Перевірити конфігурацію
cat ~/.clawdbot/clawdbot.json | grep -A5 telegram
```

**Рішення:**
```bash
# Перезапустити сервіс
systemctl --user restart clawdbot-gateway.service

# Якщо не допомагає — повне перезавантаження
clawdbot gateway --force
```

---

### Проблема 3: SSH Port Forwarding обривається

**Симптоми:** Доступ до `localhost:18789` зникає.

**Діагностика:**
```bash
# Перевірити WSL IP (може змінюватися)
ip addr show eth0 | grep inet
```

**Рішення:**
```bash
# Крок 1: Дізнатися актуальний WSL IP
wsl hostname -I

# Крок 2: Використати актуальний IP
ssh -N -L 18789:127.0.0.1:18789 root@<NEW_WSL_IP>

# Автоматичний варіант (з Windows PowerShell)
ssh -N -L 18789:127.0.0.1:18789 root@$(wsl hostname -I | awk '{print $1}')
```

**Постійне рішення (Windows PowerShell) — динамічне визначення IP:**
```powershell
# Створити скрипт ssh-forward.ps1
$wslIp = (wsl hostname -I).Trim()
ssh -N -L 18789:127.0.0.1:18789 root@$wslIp

# Додати до Task Scheduler з тригером At Startup
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-File C:\Scripts\ssh-forward.ps1"
$trigger = New-ScheduledTaskTrigger -AtStartup
Register-ScheduledTask -Action $action -Trigger $trigger -TaskName "ClawdBot SSH Forward" -Description "Auto SSH forwarding for Clawdbot"
```

---

### Проблема 4: Git конфлікти після pull

**Симптоми:**
```
CONFLICT (content): Merge conflict in config.json
```

**Рішення:**
```bash
# 1. Подивитися конфліктні файли
git status

# 2. Відкрити файл та знайти маркери конфлікту
nano config.json
# Шукати <<<<<<< HEAD ... ======= ... >>>>>>> origin/main

# 3. Видалити маркери, залишити потрібний код
# 4. Додати до staging
git add config.json

# 5. Завершити merge
git commit -m "Resolved merge conflict in config.json"

# 6. Відправити
git push
```

---

### Проблема 5: Високе споживання пам'яті

**Симптоми:** Gateway використовує >500MB RAM.

**Діагностика:**
```bash
# Перевірити поточне споживання
systemctl --user status clawdbot-gateway.service | grep Memory

# Детальна статистика
ps aux | grep clawdbot
```

**Рішення:**
```bash
# Очистити кеш
rm -rf ~/.clawdbot/memory/*.tmp

# Перезапустити з обмеженням пам'яті
systemctl --user stop clawdbot-gateway.service
# Відредагувати override.conf
systemctl --user edit clawdbot-gateway.service
# Додати: MemoryMax=512M

systemctl --user daemon-reload
systemctl --user start clawdbot-gateway.service
```

---

### Проблема 6: Втрата токена авторизації

**Симптоми:**
```
401 Unauthorized при доступі до dashboard
```

**Рішення:**
```bash
# Згенерувати новий токен
clawdbot dashboard --no-open

# Скопіювати URL з виводу
# Відкрити у браузері вручну
```

---

## 📝 Корисні команди

### Швидкий доступ

```bash
# Alias для зручності (додати до ~/.bashrc)
alias clawd='cd /root/clawd'
alias cwlogs='clawdbot logs --follow'
alias cwstatus='systemctl --user status clawdbot-gateway.service'
alias cwrestart='systemctl --user restart clawdbot-gateway.service'
alias cwdash='clawdbot dashboard'
alias cwupdate='cd /root/clawd && ./update_bot.sh'

# Після додавання
source ~/.bashrc
```

### Моніторинг ресурсів

```bash
# Real-time моніторинг процесу
watch -n 2 'ps aux | grep clawdbot | grep -v grep'

# Логи systemd
journalctl --user -u clawdbot-gateway.service -f

# Disk usage
du -sh /root/clawd/*
du -sh ~/.clawdbot/*
```

### Backup конфігурації

```bash
# Створити backup
cd /root
tar -czf clawd-backup-$(date +%Y%m%d).tar.gz \
  clawd/ \
  .clawdbot/clawdbot.json \
  .clawdbot/credentials/

# Відновити з backup
tar -xzf clawd-backup-20260130.tar.gz -C /root/
```

---

## 🎯 Best Practices

### 1. Щоденна рутина

```bash
# Ранок
./update_bot.sh          # Синхронізувати код
clawdbot doctor          # Перевірити health
clawdbot logs --limit 50 # Переглянути останні події

# Вечір
git add -A && git commit -m "Daily updates" && git push
```

### 2. Безпека

- ❌ Ніколи не комітити `.env` та `google-key.json`
- ✅ Регулярно ротувати токени
- ✅ Використовувати SSH ключі замість паролів
- ✅ Перевіряти `.gitignore` перед push

### 3. Продуктивність

- Використовувати `--thinking low` для простих запитів
- Обмежувати `--limit` при перегляді логів
- Очищувати старі log файли раз на тиждень
- Моніторити використання диску

### 4. Документування

- Додавати описові commit messages
- Оновлювати `memory/YYYY-MM-DD.md` щодня
- Переглядати `MEMORY.md` раз на тиждень
- Документувати критичні рішення у `docs/`

**Автоматизація щоденного Memory:**
```bash
# Команда для підбиття підсумків дня
clawdbot agent --message "Проаналізуй події за сьогодні та запиши summary в memory/$(date +%F).md" --thinking medium

# Додати до crontab (щовечора о 23:00)
0 23 * * * cd /root/clawd && clawdbot agent --message "Daily summary: проаналізуй сьогоднішні логи та оновлення" >> memory/$(date +\%F).md 2>&1

# Тижневий огляд (кожної неділі)
0 22 * * 0 cd /root/clawd && clawdbot agent --message "Weekly review: підсумок тижня для MEMORY.md" --thinking high
```

---

## 📞 Підтримка

**GitHub Issues:** https://github.com/nkfed/AInfoCenter/issues  
**Документація:** https://docs.clawd.bot/  
**Community:** Discord (pending)

---

*Останнє оновлення: 2026-01-30*  
*Версія документа: 1.0*  
*Автор: System Architect Team*
